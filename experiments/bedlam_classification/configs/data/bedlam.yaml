# shared
target: experiments.data.INRDatasetHMR
normalize: False
dataset_name: bedlam
dataset_dir: dataset
splits_path: bedlam_splits.json
statistics_path: bedlam_statistics.pth
num_classes: 2
img_shape: [28, 28]
inr_model:
  _target_: nn.inr.INRPerLayer
  in_features: 2
  n_layers: 3
  hidden_features: 32
  out_features: 1

stats:
  weights_mean: null
  weights_std: null
  biases_mean: null
  biases_std: null

train:
  _target_: ${data.target}
  _recursive_: True
  dataset_dir: ${data.dataset_dir}
  splits_path: ${data.splits_path}
  split: train
  normalize: ${data.normalize}
  augmentation: False
  permutation: False
  statistics_path: ${data.statistics_path}
  # num_classes: ${data.num_classes}

val:
  _target_: ${data.target}
  dataset_dir: ${data.dataset_dir}
  splits_path: ${data.splits_path}
  split: val
  normalize: ${data.normalize}
  augmentation: False
  permutation: False
  statistics_path: ${data.statistics_path}
  # num_classes: ${data.num_classes}

test:
  _target_: ${data.target}
  dataset_dir: ${data.dataset_dir}
  splits_path: ${data.splits_path}
  split: test
  normalize: ${data.normalize}
  augmentation: False
  permutation: False
  statistics_path: ${data.statistics_path}
  # num_classes: ${data.num_classes}
